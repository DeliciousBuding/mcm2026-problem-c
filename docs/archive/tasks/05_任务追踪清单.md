更新日期：2026-02-02 | 版本：v0.4.1 | 说明：英文为准，中文论文部分不在本任务范围。

# 论文冲奖任务清单（目前执行中）

目标：把当前稿件从“观感专业”推向“Outstanding”竞争级别，重点补齐“方法—指标—叙事—图表”的可信闭环。遵循下述 Block 流程执行，每完成一个 Block 就打勾（[√]），并在本清单写出验收口径和可交付产物，防止返工。

---

## 全局规范（必须遵守）

### 任务流程
更新日期：2026-02-02  
版本：v0.4.1  
说明：英文为准，中文论文部分不在本任务范围。

# 论文冲奖任务清单（进行中）

目标：把当前稿件从“观感专业”推向“Outstanding”竞争级别，重点补齐“方法 — 指标 — 叙事 — 图表”的可信闭环。遵循下述 Block 流程执行，每完成一个 Block 就打勾，并在本清单写出验收口径和可交付产物，防止返工。

---

## 全局规范（必须遵守）

**任务流程：**
- 每次执行前先写出待办项；完成后再打勾。
- 保持 Block 专一；不要在一个分支同时拆多个 Block。

**Git 规范：**
- 禁止在 main 直接提交；分支命名格式：`block{N}-{简述}`。
- 提交说明全中文，描述具体改动。
- 中断时使用 `git stash push -u -m "中文说明"` 保存工作。

**编译与验证：**
- 仅英文版需要编译。提交合并前应通过以下检查：

```powershell
cd paper
latexmk -pdf -interaction=nonstopmode main.tex
```

- 编译检查要求：0 undefined references、0 missing figures、图号/表号一致。编译失败禁止合并。
- 测试：提交前必须运行 `pytest tests/ -v`。
- 产物要求：`outputs/figures/summaries` 必须与论文引用一致；`summary_metrics.tex` 由代码生成，禁止手工改写。

---

## 当前 Block

### Block 7：论文结构与引用规范 (进行中)

**目标与说明：**
- 统一目录/附录标题格式（英文版使用 `Appendix A/B/C`）。
- 确保 Memo、Summary、AI Use Report 等顺序与最新版目录一致。
- 多引用条目按升序整理；文内引用格式统一。

**验收口径：**
- Appendix 标题在目录及正文一致，且无重叠。
- 文档顺序应为：Summary → Solution → Memo → References → AI Use Report。
- 参考文献引用排序与格式无误。

**产出：**
- 更新后的 `main.tex`（结构/目录/引用）。
- 相关的说明文档或 checklist。

---

### Block 8：工程流程梳理 (待办)

**目标与说明：**
- 复核当前分支与流程状态，确保分支命名与提交模板符合规范。
- 本清单应与归档版本（`05_任务追踪清单_归档.md`）一一对应。

**验收口径：**
- 分支与提交规则无违规。
- 清单与归档版边界清晰，可复用说明。

**产出：**
- `05_任务追踪清单_归档.md`（记录已完成内容）。
- 可复制的复核说明文档。

---

### Block 9：核心结果表简化（已完成）

- [x] 英文表中移除 “DAWS improvement in stability” 行。
- [x] 中文表中移除 “DAWS 稳定性提升” 行。

**验收口径：**
- [x] 表格不含多余条目，英中文一致。
- [x] 编译无报错。

**成果：**
- [x] 更新的 `main.tex` 和 `main_zh.tex`。

---

## 归档说明

- 所有已完成的 Block 及其细节应迁移至 `05_任务追踪清单_归档.md`。
- 归档版保存全量进度记录；本清单保留未完成与正在执行的 Block 以便当下跟踪。

---

如需我把归档版本也一并生成或将本次更新提交到 Git，请告知下一步操作。