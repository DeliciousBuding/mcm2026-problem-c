# 图表改进任务清单（从逐图评审提炼）

说明：根据 `docs/图表逐一评审.md` 提炼为可执行任务，按优先级排序。

## P0（立即修）
- 机制流向图：将 `fig_alluvial_finalists` 替换为更直观的“冠军/Top3 翻转概率条形图”，并补充图例说明。
- Pro 舞伴差异图：`fig_pro_diff_forest` 清洗重复/复合姓名（如“Emma Slater/Kaitlyn Bristowe”重复），统一显示姓名。
- Rule switch 图：在中文正文明确“探索性”性质，强调主分析采用第 28 季切换点；必要时移入附录。

## P1（本轮优化）
- Judge-save 曲线：在图注加入 \hat{\beta} 数值/区间；如有余力叠加底二分差散点或直方分布。
- Fast vs Strict 图：在图注加入 MAE / Top-1 / Top-2 一致率数值。
- PPC 图：图注明确 Coverage 高好、Brier 低好（或改为 1-Brier）。

## P2（增强叙事）
- 冲突图说明：在图注明确“点大小=HDI 宽度”，外圈=错误淘汰。
- 争议人物 ridgeline：在图注说明“每条波峰对应一周”，并考虑增加周序标注。
- Feature scatter：对长标签进行缩写或在图注列出最偏离特征。

## P3（可选/附录）
- Sigma 敏感性：若无新增信息，可移至附录或替换为更关键的参数敏感性（\epsilon/\alpha_0/\eta）。
- AUC 图：可选加入校准曲线或在图注注明训练/测试划分。

## 依赖/准备
- 若替换图表：更新 `paper/main.tex` 与 `paper/main_zh.tex` 的图引用与图注。
- 若新增统计数字：从 `outputs/summary_metrics.json` 或计算结果中抽取。

